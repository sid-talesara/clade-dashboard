import React from "react";
import Navbar from "../components/common/Navbar";

type Props = {};

const Jobs = (props: Props) => {
  return (
    <div>
      <Navbar />
    </div>
  );
};

export default Jobs;
